from fastapi import APIRouter

api_router = APIRouter()

# Placeholder sub-routers will be included here as features are developed
# from app.api.endpoints import children, conversations, chat, parent, characters
# api_router.include_router(children.router, prefix="/children", tags=["children"])
# api_router.include_router(conversations.router, prefix="/conversations", tags=["conversations"])
# api_router.include_router(chat.router, prefix="/chat", tags=["chat"])
# api_router.include_router(parent.router, prefix="/parent", tags=["parent"])
# api_router.include_router(characters.router, prefix="/characters", tags=["characters"])